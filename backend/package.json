{
  "name": "settleit-backend",
  "version": "0.1.0",
  "module": "index.ts",
  "scripts": {
    "dev": "bun run --watch index.ts",
    "start": "bun run index.ts"
  },
  "dependencies": {
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
